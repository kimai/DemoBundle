controllers:
    resource: '../../Controller/'
    type: attribute
    prefix: /{_locale}
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'

demo.api:
    resource: "@DemoBundle/API/"
    type: attribute
    prefix: /api
